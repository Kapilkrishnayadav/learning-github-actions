name: hello world
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build step
        run: echo "A demo build"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test step
        run: echo "A test step"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploy step
        run: echo "A deploy step"
